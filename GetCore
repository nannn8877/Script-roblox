repeat task.wait() until game:IsLoaded() and game.Players.LocalPlayer

local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local Player = game.Players.LocalPlayer

local isTweening = false

-- ฟังก์ชันหา target (รองรับ nested + Model)
local function getTarget(name)
    local obj = workspace:FindFirstChild(name, true) -- หาแบบลึกทั้งแมพ
    if not obj then return nil end

    if obj:IsA("Model") then
        obj = obj.PrimaryPart or obj:FindFirstChildWhichIsA("BasePart")
    end

    return obj
end

-- ปุ่มกด (เรียกใช้ได้เลย)
local function TPToTarget()
    if isTweening then return end

    local char = Player.Character or Player.CharacterAdded:Wait()
    local root = char:WaitForChild("HumanoidRootPart")

    local target = getTarget("主要部件")
    if not target then
        warn("หา object ไม่เจอ หรือไม่มี BasePart!")
        return
    end

    isTweening = true

    -- Body movers
    local bV = Instance.new("BodyVelocity")
    bV.MaxForce = Vector3.new(9e9,9e9,9e9)
    bV.Velocity = Vector3.new(0,-50,0)
    bV.Parent = root

    local bG = Instance.new("BodyGyro")
    bG.MaxTorque = Vector3.new(9e9,9e9,9e9)
    bG.P = 9e4
    bG.CFrame = root.CFrame
    bG.Parent = root

    -- noclip
    local noclipLoop
    noclipLoop = RunService.Stepped:Connect(function()
        for _, v in pairs(char:GetDescendants()) do
            if v:IsA("BasePart") then
                v.CanCollide = false
            end
        end
    end)

    -- tween ไปหา object
    local tween = TweenService:Create(root, TweenInfo.new(1), {
        CFrame = target.CFrame
    })

    tween:Play()

    tween.Completed:Connect(function()
        if noclipLoop then noclipLoop:Disconnect() end
        if bV then bV:Destroy() end
        if bG then bG:Destroy() end

        -- เปิด collide กลับ
        for _, v in pairs(char:GetDescendants()) do
            if v:IsA("BasePart") then
                v.CanCollide = true
            end
        end

        isTweening = false
        print("TP สำเร็จ")
    end)
end

-- เรียกใช้ (ลองกดรันได้เลย)
TPToTarget()
